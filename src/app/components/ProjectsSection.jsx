"use client";
import React, { useState, useRef } from "react";
import ProjectCard from "./ProjectCard";
import ProjectTag from "./ProjectTag";
import { motion, useInView } from "framer-motion";

const projectsData = [
  {
    id: 1,
    title: "Network Traffic Analyser",
    description: "Network Traffic Analyser is a tool designed to monitor, capture, and analyse network data in real-time. It helps identify patterns, detect anomalies, and troubleshoot network issues by providing insights into the type, source, and destination of network packets. The project is useful for network administrators and cybersecurity professionals to ensure secure and efficient network operations.",
    image: "/images/projects/Project1.png",
    tag: ["All", "Web"],
    gitUrl: "https://github.com/Aaditya2207/Network-Analyzer",
    previewUrl: "https://drive.google.com/file/d/1W-s4sIuEDk3VKOS0ts4vgbMrtJG3WAvX/view?usp=drive_link",
  },
  {
    id: 2,
    title: "More projects will be uploaded soon 🚀",
    description: "Projects will be uploaded here shortly",
    image: "/images/projects/test.png",
    tag: ["All"],
    // gitUrl: "/",
    // previewUrl: "/",
  },
];

const ProjectsSection = () => {
  const [tag, setTag] = useState("All");
  const ref = useRef(null);
  const isInView = useInView(ref, { once: true });

  const handleTagChange = (newTag) => {
    setTag(newTag);
  };

  const filteredProjects = projectsData.filter((project) =>
    project.tag.includes(tag)
  );

  const cardVariants = {
    initial: { y: 50, opacity: 0 },
    animate: { y: 0, opacity: 1 },
  };

  return (
    <section id="projects">
      <h2 className="text-center text-4xl font-bold text-white mt-4 mb-8 md:mb-12">
        My Projects
      </h2>
      <div className="text-white flex flex-row justify-center items-center gap-2 py-6">
        <ProjectTag
          onClick={handleTagChange}
          name="All"
          isSelected={tag === "All"}
        />
        <ProjectTag
          onClick={handleTagChange}
          name="Web"
          isSelected={tag === "Web"}
        />
        {/* <ProjectTag
          onClick={handleTagChange}
          name="Mobile"
          isSelected={tag === "Mobile"}
        /> */}
      </div>
      <ul ref={ref} className="grid md:grid-cols-3 gap-8 md:gap-12">
        {filteredProjects.map((project, index) => (
          <motion.li
            key={index}
            variants={cardVariants}
            initial="initial"
            animate={isInView ? "animate" : "initial"}
            transition={{ duration: 0.3, delay: index * 0.4 }}
          >
            <ProjectCard
              key={project.id}
              title={project.title}
              description={project.description}
              imgUrl={project.image}
              gitUrl={project.gitUrl}
              previewUrl={project.previewUrl}
            />
          </motion.li>
        ))}
      </ul>
    </section>
  );
};

export default ProjectsSection;
